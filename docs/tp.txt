Purpose of the project:-
Building very neive version of notebooklm (just textual output) for myself that can work offline with agentic feature inccuding reading imp emails and other notes or importantly telligram vit group there are a hell lot of unwanteed mesages, but i want the important ones to be notified to me. via mcp (so as to never miss important emails)


Future Plans:-
    - Choosing right vector store (that supports metadata filtering for hybrid search), compare and write about the choise
    - Hybrid searching (keywords + semantic meaning), merge both results ensembler

    - Choosing right embedding model , compare and write about choise, mteb leader for choosing right one
    - Understand more about re ranking and how to improve it, what is the reranking criteria (called 2 stage retrevial) - can use cohere re-rank api
    
    - Handling queries (querry decomposition, query rewriting, query expansion)
    
    - Offline LLM (ollama)
    - Good prompt + source showing during generation step
    - Memory module (storing chat history, redis)

    ✅ Matrices to evaluate rag and how to fix if required - Can directly use frameworks like ragas
        - Faithfulness/hallucination - (improve retrevial quality, basically re ranking and all)
        - Relevance - (prompt refining and adding grounding)
        - Context precesion, out of retrevied chunks how many were actually usefull - (re ranking chunks)
        - Context recall, how many correct chunks were rtrevied

Can have these probs and how to fix them:-
    - Empty retrevial -> re write the query and retry, use Hybrid search . if nothings workd then decline correctly that no context found
    - Low confidence retrevial -> re ranking stratagy
    - LLM timeout -> add timeout limit else change model
    - Context overloading -> predict/see context size then change the top k, to be sent to the model

==========================================================================================================

To do:
[✅] ragas implement kru so eak evaluation matric rahil pratyek stage of project sathi (ragas nahi but self made evaluation pipeline banavli)
[✅] verfity correctness of how the evalation matrices are been calculated, as of now it is good (only prob in it is using a better model, will fix later - Fixed this(used 7B param model instead of 0.5B) , result data - 260803)
[✅] fixing chunking and [] re ranking - need external lib, so not implemented yet, first fix evals ↑
(fixed the post retreval, i.e. the coise similairty wala part, by adding "hsnw:space = cosise" in the metadata while creating vectore store)

[✅]Fixed the vector_store rebuilding issue (ran the rebuild scrip in kaggle notebook using gpu, got more chunks and extracted the new vectore store here)

[] making the web all full functional (different chats sessions, different vectore stores) and mcp connections for gmail and other things
[] re ranking and query handling (querry decomposition, query rewriting, query expansion)
[] also understand about context and its lenth given to the llm, the temperature and all
[] implement guardrils



[] implement this pipeline:

        data/
        ↓
        LangChain Loaders
        ↓
        Chunking
        ↓
        Huggingface embedding model (BGE-small) 
        ↓
        Qdrant vector database
        ↓
        MultiQueryRetriever (top 10-15)
        ↓
        Reranker, Keep Top 3–5
        ↓
        Ollama 
